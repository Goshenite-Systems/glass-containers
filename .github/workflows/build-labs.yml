---
name: Build Goshenite Lab Images
on:
  push:
    branches: [main]
    paths:
      - 'labs/**'
      - 'generate-labs.py'
      - '.github/workflows/build-labs.yml'
  pull_request:
    paths:
      - 'labs/**'
      - 'generate-labs.py'
  workflow_dispatch:
  schedule:
    - cron: '40 7 * * *'   # daily 07:40 UTC (imports Kali rolling updates)

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  generate:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.gen.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - id: gen
        run: |
          python3 generate-labs.py
          # emit a JSON matrix of lab dirs
          labs=$(ls -1 labs)
          matrix=$(echo "$labs" | jq -R -s -c 'split("\n") | map(select(length>0)) | {lab: .}')
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  build:
    needs: generate
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate.outputs.matrix) }}
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.lab }}
        id: build
        uses: docker/build-push-action@v6
        with:
          context: labs/${{ matrix.lab }}
          file: labs/${{ matrix.lab }}/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/goshenite-systems/${{ matrix.lab }}-lab:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Sign ${{ matrix.lab }} with cosign
        if: github.event_name == 'push'
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
          COSIGN_YES: "true"
        run: |
          cosign version || (curl -sSfL https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64 -o /usr/local/bin/cosign && chmod +x /usr/local/bin/cosign)
          cosign sign --yes "ghcr.io/goshenite-systems/${{ matrix.lab }}-lab@${{ steps.build.outputs.digest }}"

  pull-request:
    needs: generate
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build (no push) ${{ matrix.lab }}
        uses: docker/build-push-action@v6
        with:
          context: labs/${{ matrix.lab }}
          file: labs/${{ matrix.lab }}/Dockerfile
          push: false
          tags: ghcr.io/goshenite-systems/${{ matrix.lab }}-lab:pr
